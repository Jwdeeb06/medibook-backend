// Auth controller — full implementation in Week 2 (JWT + bcrypt)
exports.register = async (req, res) => {
  res.status(501).json({ message: 'Register endpoint — coming in Week 2' });
};

exports.login = async (req, res) => {
  res.status(501).json({ message: 'Login endpoint — coming in Week 2' });
};
